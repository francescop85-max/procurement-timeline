export default function CampaignPanel({ onClose }) {
  return (
    <div className="planner-panel">
      <div className="planner-panel-title">Campaign Panel (coming soon)</div>
      <button className="action-btn" onClick={onClose}>Close</button>
    </div>
  );
}
